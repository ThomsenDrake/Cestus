# Phase 3 shared ontology: local acceptance evidence

Date: 2026-09-05. Baseline: clean `neo` at `60559c00`. Implementation branch: `codex/phase3-shared-ontology`, created in the primary checkout. No helper worktree retains `neo`. No push, PR, merge, deployment, real-record access or live transfer was performed. `SECURITY.md` and historical evidence/events were preserved.

## Status and limits

Local implementation and synthetic acceptance are recorded here. **Phase 3 is not fully accepted against real records or a live provider.** The user has not selected an authorized personal-record folder or live provider/model/private credential configuration and bounded evaluation. A bundled input question was confusing; implementation continued with fictional records without treating that reply as authorization.

The external provider is the analysis path for selected evidence. Two browser invocations below use a local protocol fixture; neither is a live model evaluation. Real-provider extraction quality, provider-specific compatibility/accounting, real document formats and scans remain pending. Partial PDF coverage is propagated through contracts, processing tests and existing reader controls; this is not OCR or complete visual coverage. The Phase 3 browser trial used text documents. Automatic pattern discovery, competing-explanation generation and evidence pursuit remain Phases 4 and 5.

## Built production browser trial

The existing production server served `npm run ui:build` output on loopback `127.0.0.1:28803`; it used a fresh disposable portable workspace at `/tmp/cestus-phase3-browser.jw438rcx/workspace`, an external private runtime configuration and the normal one-use authenticated session URL. Browser driver: existing agent-browser installation, isolated session `cestus-phase3`. No seeded accepted facts, SQL edits, browser-injected adapters, direct API mutation from the browser console or developer server were used. HTTP tests are separately identified below.

The reproducible original fictional documents are `test-data/phase3-records`. Through Ingestion controls, registered the copied folder, scanned, reviewed and approved raw import, ran import, and extracted. Five initial files became four canonical records: award register, payment register, unrelated namesake minutes and derivative report; the byte-identical award copy retained source occurrence provenance. Changing only the disposable award source later created a fifth canonical version, preserving the old original and its extraction links.

Through normal Ontology controls:

- Created award, payment and namesake investigations with questions/scope, selected their evidence memberships, and later created an entity-only investigation to check explicit entity membership. Reused evidence and entity identities across memberships.
- Manually proposed and group-accepted Alex Vale and Harbor Agency with their actual supporting passages. Added identifier `AV-101`, amount `1200`, an `awarded` relationship and an award occurrence with recipient/awarder roles, an amount attribute and occurrence month `2025-03`. Publication time remained unknown when no publication passage was selected.
- Bound the payment register's separately reviewed Alex mention to the existing AV-101 actor. The dossier showed one identity, both case memberships, two source records and two explicitly reviewed independent origins. The AV-909 same-name actor stayed distinct.
- Deliberately made a wrong cross-case binding for AV-909, then repaired it to a distinct identity. The history retained the original acceptance, wrong binding and repair, with authenticated operator identity and rationale; restart recovered that history.
- Preserved a dispute on the original `1200` assertion, edited a new proposal with the payment source's `1300`, atomically accepted the replacement and superseded the original, then withdrew the replacement. Prior values, source passages, dispute, correction and withdrawal remain inspectable.
- Created one identifier assertion supported by both independent registers and rejected a redundant proposal. Reviewed award and payment origins as independent, and the report as derived from the award. Repeated imports, mentions and memberships did not increase canonical-source or independent-origin counts.
- Corrected the original entity label to the literal `Alex`, retaining the same shared identity. The relationship table and occurrence participant display followed the current explicit binding. The entity-only case exposed its entity assertion, incoming award relationship and occurrence participation despite having no evidence membership.
- Selected the existing award occurrence identity for the derivative report. The view retained one occurrence group with two supporting assertions; the report did not become an independent event or corroborating origin. Unresolved cross-source occurrence identity is explicitly labeled, and groups are not advertised as independent event totals.
- Opened a fact's actual source passage, then reopened its stable URL after import changes and restart. Cases, selected investigation, accepted knowledge, unreviewed proposals, lineage and histories persisted. A separate manually recorded case hypothesis and its supporting assertion reference also recovered. Manual correction remained functional after provider configuration was removed.

Artifacts in `phase3-ontology/` include dossier/identity/correction snapshots, occurrence/source-independence observations, built-UI screenshots and the separate provider observations. All displayed identities and source content in these artifacts are fictional.

## Synthetic provider integration, separately bounded

A loopback-only OpenAI-compatible protocol fixture at port 28804 computed proposals from the actual selected request text. Model label: `synthetic-phase3-fixture`. Credential: disposable synthetic value kept outside the repository. The production service used its normal transport and server environment; there was no injected browser provider or accepted-data seed.

1. Used initial classification and human review to mark the authored fictional award record `public_safe` with a rationale. Selected passage 1 only, maximum output 2048 tokens, explicit budget $0.05. Inspected outgoing instructions/content, immutable source/extraction hashes, destination, model and schema-aware operation; approved the exact transfer, then ran it.
2. Read the completed persistent result: `Alex Vale`, `1200`, `2025-03`. Imported it as proposals, then separately selected and accepted the three proposals with a human rationale. Explicitly bound the provider's new mention to the already verified actor.
3. Changed the disposable source name, amount and date to `Robin Vale`, `2400`, `2026-07`; scanned/imported/extracted through normal controls, searched for Robin, opened the result and independently classified/reviewed that fictional version. A new exact preview, $0.05 budget and approval produced those changed values. Imported them as **unreviewed** proposals. No acceptance was implied by provider completion.

Exactly two loopback requests were observed. Approved caps totaled $0.10, with no paid-provider requests or live charges. `synthetic-transfer-preview.txt`, `synthetic-transfer-observations.json`, `synthetic-provider-review.txt` and `changed-source-provider-output.txt` preserve the distinction. These runs demonstrate integration and input dependence, not model reasoning quality.

## Regression, replay and review evidence

Meaningful red/characterization evidence preceded the ledger work and review fixes. Targeted tests cover both in-memory and SQLite ledgers: all-or-none batches, stable identical retry, conflicting decision identity, stale revision, concurrent decisions, second-event/receipt failure, abrupt subprocess exit before COMMIT, reopen and retry. Existing v1 records are decoded unchanged; v2 records use strict per-type versioning and persisted schema snapshots. No historical migration rewrites ledger rows or extraction artifacts.

The production HTTP regression creates its own fresh workspace and imports/extracts through supported endpoints. It checks canonical citations and authenticated commands, rejects fabricated quotes, source hashes, extraction IDs, locators, workspace IDs and unsupported value types, rejects actor spoofing and stale decisions, checks identical retry, shared membership, binding correction and preserved dispute/withdrawal. It also checks the 25-proposal boundary within the shared 40-proposal limit and verifies that both ontology endpoints hide knowledge after source quarantine.

Domain/UI/processing regressions check current dependent views, source independence, unknown vocabulary remaining proposed, unsupported units, partial dates/coverage, authorization changes during awaited reads, schema-aware provider output and failure/uncertainty handling. Focused review regressions passed 51 tests; final identity-label regressions passed 16 UI tests. Report/timeline compatibility characterization checks actual scalar values, partial occurrence times, binding provenance and disappearance of withdrawn support.

Compound Engineering was used for bounded execution, simplification, defect diagnosis, native review and browser checks. Simplification reused the atomic capability guard and avoided rebuilding an identical projection, clarified schema naming/types, and retained raw provider artifacts for provenance. Unmeasured indexing/caching proposals were deferred. The native review's four validated findings were fixed: legacy read authorization, numeric-unit grounding, current identity labels and entity-only memberships. Follow-up review closed all four; the report and closure are preserved. Cross-provider review routing stayed disabled. Additional review lenses used bounded orchestrator coverage when host agent capacity was exhausted; the receipt records that limitation.

An initial full run passed typecheck and 4089 tests but found one v1 legacy-history regression. The compatibility adapter now distinguishes policy-authorized historical whole-document provenance from the stricter Phase 2 requirements for new passage-backed proposals. The unchanged legacy fixture and current revocation/domain tests then passed together (20 tests). An additional invalid-stream regression proved that missing governance state cannot authorize legacy source history; the final targeted group passed 21 tests and follow-up review closed the compatibility change. A separate durable learning document is unnecessary: the relevant invariants and regression reasoning are captured directly in tests, the runbook and this evidence.

A final built-browser check caught producer/consumer drift in the old provenance view: its strict UI decoder rejected the added assertion value and citation-scope fields. A regression using the actual server replay DTO reproduced the failure. The focused adapter/type/rendering fix passed all six legacy UI tests and independent follow-up review. Rebuilt production UI opened the accepted relationship, corrected current label and actual assertion values successfully; v1 whole-document citations remain explicitly labeled without fabricated passage anchors. The browser had no uncaught page errors. The narrow mobile dossier screenshot was readable, but a measured 11-pixel page overflow remains a layout limitation; no zero-overflow claim is made.

## Final verification and local commits

`npm run verify` completed successfully after the final source change: typecheck passed; **270 test files passed, 3 skipped; 4,092 tests passed, 5 skipped**; both terminal assurance tests passed; production UI build passed. `git diff --check HEAD` passed. The full log is `phase3-ontology/final-verify.txt`. Non-failing output includes the missing TypeScript source-map warning, experimental localStorage warning, browser externalization of `node:crypto`, and the large UI chunk warning. Built-browser journeys supplement these checks; tests alone are not acceptance.

Local commits begin with `620f8858` (versioned events and atomic decisions) and `3b34852e` (shared knowledge domain, grounded provider path and compatibility). The subsequent UI/runbook/acceptance commit contains this record; its ID is reported in the task delivery. No push, PR or merge was performed. The owned loopback production server, synthetic provider fixture and browser session were stopped; the disposable workspace and immutable originals were retained. The primary checkout remains on the task branch, and no helper worktree holds `neo`.

## Remaining acceptance input

When ready to validate personal use, select the exact local source folder and relevant investigation questions/formats first. Then choose the provider endpoint/model and private credential configuration location (never paste a key into the task), followed by the exact eligible passage/document selection and bounded total evaluation budget. Each live transfer still requires current classification, exact preview and approval in the product. Do not reclassify sensitive evidence merely to permit processing. Until those checks are performed, real-record and AI-to-review live acceptance remain pending.


## Focused Phase 3 follow-up: vocabulary, written dates and mobile layout

Follow-up baseline: `3789787a` on `codex/phase3-shared-ontology`; the working tree was clean before these changes. The accepted Phase 3 direction and previous acceptance evidence above were retained. The bounded work added human-reviewed vocabulary extensions, shared explicit date normalization and the measured mobile overflow fix. No Phase 4/5 work, new provider framework or historical migration was introduced. Full Phase 3 acceptance remains pending the real-record/live-provider checks.

### Implementation and review

`extendSchema` is an authenticated human domain command with an exact base schema and workspace revision, stable decision identity, current authorization and atomic conditional batch append. Its concrete preview precedes approval. Each additive definition and rationale is recorded alongside a new immutable schema snapshot. Existing definitions cannot be changed; names, uniqueness, scalar/relationship kinds, supported endpoint references and endpoint constraints are validated. Unknown model vocabulary cannot extend itself or accept knowledge. A prior unknown-term proposal becomes reviewable only through an explicit new proposal referencing both its original and the reviewed active schema, followed by a separate human acceptance. Old accepted records retain their snapshot meaning.

Manual and provider candidates share `written-date.v1` normalization with original source expression and exact citation index. Unambiguous English written dates preserve year/month/day precision; calendar-invalid, ambiguous numeric, relative, unsupported-locale and missing-year inputs are rejected rather than guessed. ISO grounding has token boundaries. Occurrence and publication time remain separate. Mixed-precision intervals use earliest-start/latest-end bounds to reject definitely reversed intervals without filling in unknown days. Canonical source/extraction hashes, locator, exact quotation and current evidence authority remain required. Provider prompt v2 and previews use the reviewed active schema; stale schema changes prevent approval, transmission or publication as applicable, while old artifacts keep their original snapshots and prompt versions.

CE simplification reused the ledger's last recorded schema directly instead of rebuilding the graph. Native code review found and closed two regressions: new boolean occurrence attributes had become strings in the form, and stricter grounding had retroactively blocked correction of historical partial dates. The form now preserves boolean types during creation/editing and excludes subject-restricted facts from occurrence attributes. Existing lifecycle/reference commands revalidate canonical citations and current authority; new proposals and acceptance still enforce strict grounding. Historical rows are unchanged. Review coverage included an independent correctness reviewer and a fresh validator; other selected lenses used disclosed parent-context fallback after host thread limits. Cross-provider routing remained disabled. Receipts are `phase3-followup/review-review.json` and `review-closure.json`; both findings are closed. No separate CE learning qualifies: the historical interpretation/authority distinction and its rationale are already explicit in the code, regression tests, runbook and this record.

Red evidence includes the previously unsupported schema command, stale provider schema handling, written-date output rejection, boolean attribute type loss, and four historical lifecycle failures. The final targeted group passed **116 tests across five files** with no unhandled errors: domain, date, provider, production HTTP and UI contracts. The new edit test initially exposed jsdom's missing `scrollIntoView`; an element-local test stub resolved that test-environment issue without changing browser behavior. Exact red and green logs are in `phase3-followup/`.

### Fresh built production browser trial

Production `npm run local:runtime` served the built UI at loopback port 28823 using a fresh disposable workspace `/tmp/cestus-phase3-followup.jwx3nlh1/workspace` and private configuration outside the checkout. The isolated agent-browser session was `cestus-phase3-followup`. The authored fixture is `test-data/phase3-followup-records/register.txt`; it was copied, registered, scanned, approved, imported and extracted through normal controls. No seeded accepted knowledge, SQL changes, injected browser adapters or browser-console API mutations were used.

Through ordinary controls:

1. Created **Synthetic cooperative inquiry**, question/scope and evidence membership. Proposed the unfamiliar `cooperative` entity type for Harbor Circle; acceptance was blocked. Inspected and approved its bounded addition, creating schema v2. The original proposal remained on v1 and could not be accepted under changed schema meaning. **Edit as new proposal** created a grounded v2 proposal; a separate human decision accepted it.
2. Proposed an unfamiliar date predicate `registered_on`, with **September 5, 2026** displayed as **2026-09-05** beside the source passage. Acceptance was blocked while the predicate was unknown. Inspected and approved a typed date predicate restricted to cooperative subjects, creating v3. Explicitly revised the proposal, then separately accepted it. Source expression, interpretation method, quotation reference and day precision remained visible in history.
3. Accepted a meeting occurrence with occurrence day **2026-09-05** and publication month **2026-09**, independently normalized from the written expressions in the same register. No publication day was invented. A duplicate vocabulary definition was rejected. Changing investigation selection after preview made a `registry` addition stale; approval failed and the term was not added.
4. Restarted and recovered schema v3, the cooperative, accepted date/occurrence and older proposals. After the provider trial below, restarted again without provider configuration, recovered the same manual and provider knowledge/history, opened the manual date in the narrow edit form, and reopened its stable source URL to the exact original passage. The reader showed no fallback provider configured.

One early browser-driver attempt targeted the wrong field while selecting the unfamiliar entity type, producing a default `person` proposal. This was discovered in the dossier and withdrawn through normal controls with an explanation. It remains in history. The unknown-type rejection above was then repeated using the observed field reference; the mistaken entry is not counted as that rejection.

The loopback fixture at port 28824 used the actual request passage to produce a cooperative entity, registered date and occurrence/publication dates. Its model label was `synthetic-written-date-fixture`. The authored fictional record was deliberately classified/reviewed as `public_safe`; passage 1 only, 2048 output tokens and $0.05 cap were previewed, separately approved, then run. Exactly **one** loopback request was observed. The preview showed the reviewed v3 schema including `cooperative` and `registered_on`; returned proposals carried the written expressions and interpreted dates. Results were imported as unreviewed proposals. The entity and date were separately accepted by the human session, and its entity mention was explicitly bound to the existing cooperative. The fixture occurrence remains unreviewed. This is synthetic integration evidence, not live-model acceptance, quality or billing evidence; no paid provider was invoked.

Full snapshots preserve provider result, subsequent human review, source recovery and restart state. Some intermediate driver snapshots list interactive controls only; they supplement the recorded journey and focused regressions rather than independently showing every error message. The private session URL, credential configuration and server log are excluded from committed evidence.

### Mobile defect closed

The original overflow was the long occurrence identity paragraph: its text reached 400.547px at a 390px viewport. The focused fix allows that paragraph to wrap. Built browser measurements on a disposable copy of the prior multi-case workspace show **390px document width at 390px viewport**, both all-cases and Alex dossier; relationship tables retain their own horizontal scrolling and content. The new fresh-workspace dossier, vocabulary relationship preview and written-date edit form also measured **390/390**. Screenshots show the full addition definition, source quotation and written-date interpretation without page clipping. The old 11px limitation above is historical and is resolved by this follow-up. No uncaught browser errors were recorded.

### Follow-up final verification

`npm run verify` passed after the final code changes: typecheck; **271 test files passed, 3 skipped; 4,155 tests passed, 5 skipped**; both assurance tests; production UI build. The complete log is `phase3-followup/final-verify.txt` (terminal escape sequences and trailing whitespace removed only). The known non-failing source-map, localStorage, browser `node:crypto` externalization and chunk-size warnings remain. `git diff --check` passed.

The owned follow-up browser, production server and synthetic fixture were stopped. The selected fictional workspaces, immutable originals and review history are retained; no running user workspace was modified. No helper worktree holds `neo`; `SECURITY.md` is unchanged. Domain/provider changes are committed as `95e5f5e0`; the following UI/runbook/evidence commit ID is reported in the task delivery. No push, PR, merge or deployment was performed. Real-record and authorized live-provider acceptance remain pending.
