# Provisional synthetic acceptance corpus

These are invented records across three cases, not application seed data. Nothing loads them automatically. `manifest.json` records hashes, counts, expected identities, conflicts, negative controls, and a withheld record that defeats the initially favored gap explanation.

Phase 1 exercises workspace/session/PRR durability only. Text and CSV are inspectable fixture formats, **not a claim of implemented extraction**. The truncated PDF and unknown extension must never acquire a successful extraction label. There are no scans or valid PDF pages. Real cases, formats, page counts, sensitivity, and capacity targets remain provisional until the investigator supplies them. The proposed 100-file/10 MB growth fixture is not a capacity guarantee.

For later analysis acceptance, start with `case-*`, keep `withheld` separate, and use `unsupported` for failure handling. Retain both award register versions and both occurrences of the duplicate notice. Cases organize working sets over shared identities; they do not define separate ontologies. No cross-case inference or inquiry engine is implemented by these files.
